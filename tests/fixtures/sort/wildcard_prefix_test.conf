# # # Test file for wildcard prefix stanzas
# # # Type wildcard stanza
[host::*]
setting = value1

# # # Type wildcard prefix stanza
[host::*-webserver]
category = web
monitoring = enabled

# # # Another type wildcard prefix stanza
[source::*-access_log]
sourcetype = access_combined
TRUNCATE = 8000

# # # Type specific stanza for comparison
[host::webserver1]
ip = 192.168.1.1 

